<div class="osusume" style="background-color: 
  <%= @source == 'tsubuyaki' ? '#d6f59c' : '#d6f59c' %>;">
  <div class="smile">
    <h2><%= @heart.body %></h2>
    <p><%= @heart.created_at %></p>
      <% if user_signed_in? %>
        <% if current_user.already_liked?(@heart) %>
          <%= link_to heart_like_path(@heart), method: :delete do %>
            <h3>♥</h3><%= @heart.likes.count %>
          <% end %>
      <% else %>
          <%= link_to heart_likes_path(@heart), method: :post do %>
            <h3>♡</h3><%= @heart.likes.count %>
          <% end %>
        <% end %>
      <% else %>
        <p>いいねの数 = </p><%= @heart.likes.count %>
      <% end %>

    <br><%= link_to "編集する", edit_heart_path(@heart.id) %></br>

  <h3>✧♥✧♥✧♥(⋈◍＞◡＜◍)✧♥いいねしたユーザー♥✧(⋈◍＞◡＜◍)♥✧♥✧♥✧</h3>
  <% @heart.liked_users.each do |user| %>
    <li><%= user.email %></li>
  <% end %>

  <div class ="sakuichi">
    <div class ="sakujo">
      <%= button_to "削除する", heart_path(@heart.id), method: :delete %>
    </div>
    <div class ="ichirann">
      <%= link_to "一覧に戻る", hearts_path %>
    </div>
  </duv>
  </div>
</div>